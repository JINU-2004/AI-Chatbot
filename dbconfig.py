MYSQL_HOST = "localhost" #This variable specifies the hostname or IP address of the MySQL database server. In this case, it's set to "localhost", which means the database server is expected to be running on the same machine where the code is executed.
MYSQL_USER = "root"  #This variable specifies the username used to authenticate the connection to the MySQL server.
MYSQL_PASSWORD = ""  #This variable specifies the password used to authenticate the connection.Here no password is being used. 
MYSQL_DB = "db_edithnew" # This variable specifies the name of the MySQL database to be used. 
MYSQL_PORT = 3306 #This variable specifies the port number where the MySQL server is listening for connections. The default port for MySQL is 3306, which is what's specified here.